// Ported from tree-silhouette.html — generative 2D-canvas room, no audio.
// Adaptations: self-driving rAF becomes the shared raf loop; the hint + restart
// chrome moves to an overlay; preview mode seeds one fully-grown tree since
// there is no gesture in a card.

export { mount } from './mount';
